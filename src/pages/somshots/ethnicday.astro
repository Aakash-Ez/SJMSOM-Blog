---
import { Icon } from 'astro-icon/components';

import Image from '~/components/common/Image.astro';
import PostTags from '~/components/blog/Tags.astro';
import SocialShare from '~/components/common/SocialShare.astro';

import { getPermalink } from '~/utils/permalinks';
import { getFormattedDate } from '~/utils/utils';

import type { MetaData, Post } from '~/types';
import { boolean } from 'astro/zod';
import merge from 'lodash.merge';
import Layout from '~/layouts/Layout.astro';
import PageLayout from '~/layouts/PageLayout.astro';

const metadata = merge({
  title: 'Ethnic Day',
  description:
    "SJMSOM vibrated with cultural pride as the institute celebrated a vibrant Ethnic Day. Students donned traditional attire from across India, showcasing the nation's rich tapestry through a dazzling display of colors and customs.",
}) as MetaData;
---

<PageLayout metadata={metadata}>
  <section class="py-8 sm:py-16 lg:py-20 mx-auto">
    <article>
      <header class="">
        <h1
          class="px-4 sm:px-6 max-w-3xl mx-auto text-4xl md:text-5xl font-bold leading-tighter tracking-tighter font-heading"
        >
          Ethnic Day!
        </h1>
        <p
          class="max-w-3xl mx-auto mt-4 mb-8 px-4 sm:px-6 text-xl md:text-xl text-muted dark:text-slate-400 text-justify"
        >
          SJMSOM vibrated with cultural pride as the institute celebrated a vibrant Ethnic Day. Students donned
          traditional attire from across India, showcasing the nation's rich tapestry through a dazzling display of
          colors and customs.
        </p>

        {
          (
            <Image
              src="~/assets/images/somshots/ethnicday/1.JPG"
              class="max-w-full lg:max-w-[900px] mx-auto mb-6 sm:rounded-md bg-gray-400 dark:bg-slate-700"
              widths={[400, 900]}
              sizes="(max-width: 900px) 400px, 900px"
              alt="Ethnic Day 1"
              width={900}
              height={506}
              loading="eager"
              decoding="async"
            />
          )
        }
        {
          (
            <Image
              src="~/assets/images/somshots/ethnicday/2.JPG"
              class="max-w-full lg:max-w-[900px] mx-auto mb-6 sm:rounded-md bg-gray-400 dark:bg-slate-700"
              widths={[400, 900]}
              sizes="(max-width: 900px) 400px, 900px"
              alt="Ethnic Day 1"
              width={900}
              height={506}
              loading="eager"
              decoding="async"
            />
          )
        }
        {
          (
            <Image
              src="~/assets/images/somshots/ethnicday/3.JPG"
              class="max-w-full lg:max-w-[900px] mx-auto mb-6 sm:rounded-md bg-gray-400 dark:bg-slate-700"
              widths={[400, 900]}
              sizes="(max-width: 900px) 400px, 900px"
              alt="Ethnic Day 1"
              width={900}
              height={506}
              loading="eager"
              decoding="async"
            />
          )
        }
        {
          (
            <Image
              src="~/assets/images/somshots/ethnicday/4.JPG"
              class="max-w-full lg:max-w-[900px] mx-auto mb-6 sm:rounded-md bg-gray-400 dark:bg-slate-700"
              widths={[400, 900]}
              sizes="(max-width: 900px) 400px, 900px"
              alt="Ethnic Day 1"
              width={900}
              loading="eager"
              decoding="async"
            />
          )
        }
        {
          (
            <Image
              src="~/assets/images/somshots/ethnicday/5.JPG"
              class="max-w-full lg:max-w-[900px] mx-auto mb-6 sm:rounded-md bg-gray-400 dark:bg-slate-700"
              widths={[400, 900]}
              sizes="(max-width: 900px) 400px, 900px"
              alt="Ethnic Day 1"
              width={900}
              loading="eager"
              decoding="async"
            />
          )
        }
      </header>
      <div
        class="mx-auto px-6 sm:px-6 max-w-3xl prose prose-lg lg:prose-xl dark:prose-invert dark:prose-headings:text-slate-300 prose-md prose-headings:font-heading prose-headings:leading-tighter prose-headings:tracking-tighter prose-headings:font-bold prose-a:text-primary dark:prose-a:text-blue-400 prose-img:rounded-md prose-img:shadow-lg mt-8 prose-headings:scroll-mt-[80px]"
      >
      </div>
      </div>
    </article>
  </section>
</PageLayout>
